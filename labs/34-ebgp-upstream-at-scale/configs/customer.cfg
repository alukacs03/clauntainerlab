hostname customer
!
ip routing
!
interface Loopback0
   ip address 10.0.0.1/32
!
interface Ethernet1
   description ixp-lan
   no switchport
   ip address 198.51.100.1/24
!
! Simulate your prefix that you originate
ip route 203.0.113.0/24 Null0
!
! ─────────────────────────────────────────────────────────────
! Starter: You're physically connected to the IXP fabric. No BGP
! configured yet. Your tasks (see README):
!   1. Establish one eBGP session to the IXP route server (AS 65535
!      at 198.51.100.250) — multilateral peering pattern
!   2. Apply standard inbound filters (no bogons, no your-own-prefix,
!      max-prefix limit per the IXP's policy)
!   3. Advertise your /24 outbound
!   4. Verify you learn routes from peer1 and peer2 via the RS without
!      direct bilateral sessions to either
! ─────────────────────────────────────────────────────────────
!
end
