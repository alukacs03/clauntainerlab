hostname cgn
!
ip routing
!
! VLAN 100 = the shared CGN subscriber segment (100.64.0.0/22).
! All three subscriber-facing ports are access ports in this VLAN,
! so every subscriber shares one gateway (the Vlan100 SVI). This is
! the "shared transit" CGN model.
vlan 100
!
interface Ethernet1
   description outside-toward-internet
   no switchport
   ! Primary point-to-point link to the internet node
   ip address 203.0.113.2/30
   ! Dynamic source PAT (overload) is applied UNDER the interface in EOS —
   ! there is no global "ip nat source list" / "ip nat enable" (Cisco IOS).
   ! In EOS `overload` PATs every subscriber onto THIS interface's single
   ! public IP (203.0.113.2), keyed by port. A multi-IP CGN *pool* (carrier-
   ! scale NAT444 across several public IPs) is NOT expressible as
   ! `pool ... overload` on EOS/cEOS — it needs a dedicated CGN platform.
   ! See the README callout; here we demonstrate the PAT mechanism on one IP.
   ip nat source dynamic access-list CGN-INSIDE overload
!
interface Ethernet2
   description sub1 (CGN subscriber)
   switchport access vlan 100
!
interface Ethernet3
   description sub2 (CGN subscriber)
   switchport access vlan 100
!
interface Ethernet4
   description sub3 (CGN subscriber)
   switchport access vlan 100
!
interface Vlan100
   description shared CGN subscriber gateway (100.64.0.0/22)
   ip address 100.64.0.1/22
!
ip route 0.0.0.0/0 203.0.113.1
!
! Match all RFC 6598 shared-CGN traffic.
ip access-list CGN-INSIDE
   10 permit ip 100.64.0.0/10 any
!
end
