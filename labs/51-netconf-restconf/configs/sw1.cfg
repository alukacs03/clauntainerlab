hostname sw1
!
username admin privilege 15 secret 0 admin
!
ip routing
!
interface Ethernet1
   description client
   no switchport
   ip address 10.0.0.1/24
!
! ── Tasks ──
! 1. Enable NETCONF over SSH (port 830)
! 2. Enable eAPI HTTPS (Arista's RESTCONF-equivalent) on port 443
! 3. Use ncclient or curl from the client to do a get-config
! 4. Push a config change via NETCONF and verify
!
end
