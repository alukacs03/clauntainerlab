hostname sw1
!
username admin privilege 15 secret 0 admin
!
ip routing
!
interface Ethernet1
   description collector
   no switchport
   ip address 10.0.0.1/24
!
! ─────────────────────────────────────────────────────────────
! Starter: device with mgmt connectivity to the telemetry
! collector. Your tasks:
!   1. Enable the gNMI server (the "management api gnmi" stanza)
!   2. Enable OpenConfig and EOS-native model exposure
!   3. From collector, use `gnmic` to subscribe to interface
!      counters and verify push-based streaming works.
! ─────────────────────────────────────────────────────────────
!
end
