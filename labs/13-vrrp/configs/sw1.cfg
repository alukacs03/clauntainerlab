hostname sw1
!
ip routing
!
vlan 10
   name USERS
!
vlan 20
   name SERVERS
!
interface Ethernet1
   description h1
   switchport mode access
   switchport access vlan 10
   spanning-tree portfast
   spanning-tree bpduguard enable
!
interface Ethernet2
   description trunk-to-sw2
   switchport mode trunk
   switchport trunk allowed vlan 10,20
!
interface Ethernet3
   description h-server
   switchport mode access
   switchport access vlan 20
   spanning-tree portfast
   spanning-tree bpduguard enable
!
interface Ethernet4
   description trunk-to-sw2 (VLAN 20 path)
   switchport mode trunk
   switchport trunk allowed vlan 20
!
interface Vlan10
   description USERS gateway (LOCAL ONLY — no VRRP yet)
   ip address 10.10.10.251/24
!
interface Vlan20
   ip address 10.20.20.254/24
!
! ─────────────────────────────────────────────────────────────
! Starter: sw1 IS the gateway for VLAN 10 (10.10.10.251) and the
! ONLY gateway. If sw1 fails, every host in VLAN 10 loses its
! default route. There is no failover.
!
! Your tasks (see README):
!   1. Configure VRRP on sw1's Vlan10 SVI:
!      - Group 10, virtual IP 10.10.10.254
!      - Priority 110 (primary)
!      - Preempt enabled
!   2. Do the same on sw2 (priority 100, secondary)
!   3. Reconfigure hosts (already done in topology) to use 10.10.10.254
!   4. Test failover: kill sw1, verify h1 keeps reaching h-server
! ─────────────────────────────────────────────────────────────
!
end
