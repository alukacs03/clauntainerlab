hostname sw2
!
! ─────────────────────────────────────────────────────────────
! Starter config. Mirror what you did on sw1:
!   - same two VLANs (10/USERS, 20/DEVS)
!   - Ethernet1 access VLAN 10  (connects to h3)
!   - Ethernet2 access VLAN 20  (connects to h4)
!   - Ethernet3 trunk, allowed VLANs 10,20  (to sw1)
! ─────────────────────────────────────────────────────────────
!
end
