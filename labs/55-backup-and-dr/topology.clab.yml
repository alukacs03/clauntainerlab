name: backup-and-dr

topology:
  kinds:
    arista_ceos:
      image: ceos:4.35.4M
    linux:
      image: ghcr.io/srl-labs/network-multitool

  nodes:
    sw1:
      kind: arista_ceos
      startup-config: configs/sw1.cfg

    backup-server:
      kind: linux
      exec:
        - ip addr add 10.0.0.100/24 dev eth1
        - mkdir -p /backups

  links:
    - endpoints: ["backup-server:eth1", "sw1:eth1"]
